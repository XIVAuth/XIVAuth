<%= render 'character_registrations/wizard/disambiguation_step',
           candidates: @character_registration.candidates,
           name: @character_registration.search_name,
           world: @character_registration.search_world %>
