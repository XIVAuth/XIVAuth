<header class="page-header">
  <h1><%= t('doorkeeper.authorized_applications.index.title') %></h1>
</header>

<main role="main">
  <table class="table table-striped">
    <thead>
    <tr>
      <th><%= t('doorkeeper.authorized_applications.index.application') %></th>
      <th><%= t('doorkeeper.authorized_applications.index.created_at') %></th>
      <th></th>
    </tr>
    </thead>
    <tbody>
    <% @applications.each do |application| %>
      <tr>
        <td><%= application.name %></td>
        <td><span data-controller="utilities--hover-dt"
                  data-utilities--hover-dt-ts-value="<%= application.created_at.to_i %>"
                  data-utilities--hover-dt-format-value="short"><%= application.created_at.strftime(t('doorkeeper.authorized_applications.index.date_format')) %></span></td>
        <td><%= render 'delete_form', application: application %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
</main>
